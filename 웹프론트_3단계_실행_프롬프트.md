# 3단계: 신규 웹 프론트엔드 구축 — 실행 프롬프트

> Sonnet으로 실행 권장. 실행 전 Shift+Tab으로 auto-accept edits 모드를 켤 것.
> 범위·설계 근거는 `.claude/skills/golf-platform/SKILL.md` 2·3단계 절이 단일 진실 원천 — 시작 전에 반드시 읽을 것.

---

golf-platform 3단계(신규 웹 프론트엔드)를 구축한다. `server/`(FastAPI) + `web/`(React+Vite+TS)를 신설하고, 기존 `golf_swing_analyzer/`(Streamlit)는 reference implementation으로 유지한다. 각 단계 완료 시 즉시 git commit & push(한국어 메시지, 의미 단위 커밋), **디자인 시안 선택(1단계)을 제외하면** 중간 확인 질문 없이 끝까지 진행한다.

## 절대 규칙
- `golf_swing_analyzer/analyzer/`는 무변경 — 예외는 단 하나, 디자인 시안 확정 후 사용자가 새 페이즈 팔레트를 선택한 경우에만 `drawing.py`의 `PHASE_COLORS` 색값(로직 아님)을 웹과 동기 변경
- 대표 프레임 선택 로직(임팩트=첫 프레임, 다운스윙=85%, 나머지=중간 — `ui/tab_analysis.py` L.262-266)은 golf-code-change A8 불변식 — 서버로 복제할 때 **문자 그대로**
- 서버 응답의 페이즈명은 한국어 원본 유지("어드레스" 등) — 번역은 프론트의 코드 키 매핑 테이블(address/backswing/top/downswing/impact/follow_through/finish)로만
- 세션 키·분석 코어 시그니처 변경 금지

## 0단계: server/ 구축

`server/` 디렉터리에 FastAPI 앱 생성. `golf_swing_analyzer/`를 sys.path에 추가해 Streamlit과 동일하게 `from analyzer.pipeline import ...`로 import (analyzer 내부가 상대 import라 이 방식이어야 함).

| 엔드포인트 | 입력 | 출력 |
|---|---|---|
| `POST /analyze` | multipart 영상 + `start_sec`/`end_sec`(옵션, 수동 트리밍) + `sample_rate`(기본 3) | `score`, `issues`, `summary`, `frame_data`, `phase_boundaries`, `fps`, `eff_sample`, `wrist_y_history`(차트용), `rep_frames`(7장, phase명→base64 JPEG) |
| `POST /auto-window` | multipart 영상 | `start_sec`, `end_sec` (`auto_detect_swing_window()` 호출) |
| `POST /detect-phases` | `{wrist_y: number[]}` | `phase_boundaries` — `SwingPhaseDetector`에 시계열 주입 후 `detect_all_phases()`. 5단계 실시간 대비 선구현 |
| `POST /coaching` | `{summary, issues, provider, api_key, model_name}` | `{feedback}` — 키는 로깅·저장 금지 |

- annotated_frames 전량 반환 금지 — 대표 7장만 base64
- CORS 허용(개발: localhost, 이후 배포 도메인)
- 분석은 동기 응답(10~20초) — 프론트에서 스피너 처리, 작업 큐는 도입하지 않는다
- requirements: `server/requirements.txt` (fastapi, uvicorn, python-multipart + 기존 분석 의존성)

**0단계 검증(동일성 게이트)**: 검증 스크립트를 작성해 `golf_swing_analyzer/video/` 3종을 ① API 경유 ② `analyzer/` 직접 호출 두 경로로 분석하고 score·phase_boundaries·frame_data 길이가 일치하는지 대조. 직렬화 버그(numpy 타입, 한글 인코딩) 검출이 목적. 통과 후 커밋.

## 1단계: web/ 스캐폴드 + 디자인 시안 (유일한 승인 게이트)

- `web/`에 Vite + React + TypeScript 스캐폴드
- **완전 새 디자인** — 기존 다크그린 테마에 앵커링하지 않는다. 서로 다른 방향의 디자인 시안 2개(예: 라이트 스포츠-테크 vs 다크 프리미엄)를 실제 랜딩+결과 화면 목업으로 만들어 제시하고 **사용자 선택을 기다린다**. 차트·색 작업 전에 dataviz 스킬을 로드할 것
- 7페이즈 색상: 기본은 기존 `PHASE_COLORS` 7색 계승(서버가 그리는 대표 프레임 오버레이와 일치해야 하므로). 시안이 새 팔레트를 요구하면 drawing.py 색값 동기 변경안을 시안과 함께 제시
- 시안 확정 후 커밋

## 2단계: 핵심 플로우 구현

업로드 → 트리밍 → 분석 → 결과:
- 업로드: 드래그&드롭, mp4/mov/avi
- 트리밍: 업로드 직후 `/auto-window` 호출로 자동 감지값을 슬라이더 초기값으로. 수동 조정은 **HTML5 `<video>`의 currentTime을 슬라이더에 바인딩**해 서버 왕복 없이 프레임 미리보기 (이게 Streamlit 대비 이 기능이 싸지는 이유 — 서버 프레임 캡처 API를 만들지 말 것)
- 분석: `/analyze` 호출, 진행 스피너
- 결과 화면: 점수 링 + 등급, 메트릭 카드(척추각/X-Factor/어깨/골반/페이즈 수), 항목별 진단 리스트, 7단계 대표 프레임 그리드, 손목Y 페이즈 경계 차트, 관절 각도 시계열 차트 (Recharts 권장, dataviz 스킬 적용)
- Streamlit Tab1+Tab2를 한 화면 흐름으로 통합. Tab5(기준 학습)·CSV는 만들지 않는다

## 3단계: AI 코칭 + 샘플 프리로드

- 코칭 화면: 공급자/모델 선택 + 사용자 API 키 입력(localStorage 저장 여부는 체크박스로 사용자 선택) → `/coaching` 호출 → 리포트 렌더
- 샘플 프리로드: 3종 영상의 `/analyze` 응답을 정적 JSON으로 `web/public/samples/`에 번들. 랜딩에 "샘플 결과 보기" 진입점 — 서버·키 없이 결과 화면 전체를 체험 가능하게

## 4단계: 한/영 토글 (i18n)

- 문자열 딕셔너리 방식(react-i18next 또는 경량 자체 구현) — 모든 UI 문자열을 처음부터 키로 작성했어야 하므로 2단계부터 하드코딩 금지
- 페이즈명은 코드 키 매핑 테이블로 표시 번역. 서버 응답은 한국어 원본 그대로
- LLM 코칭 리포트는 생성 언어 자체가 콘텐츠 — 코칭 요청 시 현재 UI 언어를 프롬프트에 전달해 해당 언어로 생성되게 `/coaching`에 `lang` 파라미터 추가 (build_prompt 수정은 하지 말고 서버에서 프롬프트 뒤에 언어 지시 한 줄 추가)

## 5단계: 최종 검증 + 문서/스킬 동기화

1. 전체 플로우 수동 시나리오: 업로드→자동+수동 트리밍→분석→결과→코칭(키 있으면)→샘플 프리로드→한/영 토글
2. 3종 동일성 게이트 재실행
3. 문서 동기화: CLAUDE.md에 `server/`+`web/` 구조 추가, golf-ui-ux 스킬에 web/ 구조·i18n 규칙 추가, golf-platform 3단계에 완료 기록. **이동·신설로 낡아진 줄번호 참조가 있으면 갱신** (전례: 두 차례 분리 때마다 스킬 줄번호 대량 무효화)
4. 완료 보고: 커밋 목록 / 구조 요약 / 검증 결과 / 미해결 이슈

배포는 이 프롬프트 범위 밖 — 로컬 완료가 이번 목표다.
