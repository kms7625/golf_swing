# 골프코칭 AI 프로젝트 — 스킬세트 생성 프롬프트

> 아래 내용을 새 세션(또는 현재 세션)에 그대로 붙여넣어 실행한다.

---

골프 스윙 AI 코칭 프로젝트의 **전용 스킬세트를 설계·생성**해줘. 코드 수정은 아직 하지 말고, 스킬세트가 이 프로젝트의 개발 전 과정을 관장하도록 만드는 것이 이번 작업의 목표야.

## 1. 배경 자료 (먼저 전부 읽을 것)

1. `CLAUDE.md` — 현재 코드베이스(단일 파일 `golf_swing_analyzer/app_v2.py`)의 구조, 7단계 스윙 감지 알고리즘의 불변식(임팩트=argmax 방식, local_to_hist 인덱스 매핑, edge-aware 스무딩, 한글 HUD는 PIL 렌더링, 손목 Y는 원시 픽셀 좌표 사용 등)
2. `Intelligent_Golf_Swing_AI.pptx` — 프로젝트 비전: 단순 수치 나열이 아닌 **원인→결과를 설명하는 Explainable AI 코치**, 서버 없이 **브라우저/스마트폰 온디바이스 30fps 실시간 분석**, 이동평균 필터·보간·CLAHE로 모션블러/조명 노이즈 극복
3. `golf_swing_analyzer/skills/kis-*/SKILL.md` — 다른 프로젝트(KIS 자동매매)에서 검증된 스킬세트. **이 패턴을 그대로 이식**한다:
   - description에 사용자가 실제로 말할 트리거 문구를 따옴표로 나열 + 다른 스킬과의 관할 경계 명시
   - "왜 이 체크리스트가 필요한가" — 실제 사고 사례/함정 나열
   - 체크리스트 각 항목에 근거를 `파일명 L.줄번호` 형식으로 부착
   - 위반 시 처리 규칙 (위반이면 수정안을 내지 않고 위반 사항부터 보고)
   - 고정된 출력 형식 블록

## 2. 프로젝트 목표 (스킬 설계에 반영할 것)

- **UI 재설계**: 현재 Streamlit UI가 불만족스러움. 웹페이지 UI부터 분석 결과물 표현까지 전면 개선
- **분석 품질**: 실제 영상을 넣었을 때 결과물(단계 감지 정확도, 각도 수치, 코칭 리포트)의 신뢰성
- **실시간 분석**: 업로드 영상뿐 아니라 실시간 카메라 영상 분석 지원 (PPTX의 온디바이스 방향 고려)
- **멀티플랫폼**: 웹페이지 + 모바일 애플리케이션 양쪽에서 사용

### 확정된 아키텍처 방향 (2026-07-04 결정) — "코어 보존 + 껍데기 교체"

"기존 개선 vs 처음부터 재작성" 논의 끝에 아래 방향으로 확정했다. 스킬 설계와 로드맵은 이 방향을 전제로 한다:

1. **분석 코어(Section 0~8)는 보존** — 기준 DB(Section 0), 관절각 수학, 7단계 감지, 스무딩/보간, 점수 계산, LLM 피드백 데이터 구성(Section 8)까지 포함 — 실제 버그를 겪고 얻은 불변식의 집합이므로 재작성 금지. 로직 변경 없이 `app_v2.py`에서 모듈(`analyzer/` 패키지 등)로 분리만 한다 (Section 9 UI만 교체 대상 — CLAUDE.md 섹션맵에 Section 0이 누락돼 있어 "Section 2~7"로 잘못 표기하기 쉬우니 주의)
2. **Streamlit 껍데기(Section 9)는 교체** — 실시간 카메라·모바일 앱은 Streamlit으로 구조적으로 불가능. 새 프론트엔드(웹 → 앱 순)를 신규 구축하고, 분석 코어는 API 또는 JS 포팅으로 연결
3. **기존 Streamlit 앱은 검증 기준(reference implementation)으로 유지** — 테스트 영상 3종 + reference_db.json으로 새 구현의 회귀 검증에 사용
4. 현재 단계 감지는 전체 영상을 다 본 뒤 후처리(post-hoc)로 도는 구조 — 실시간용 스트리밍 감지는 별도 설계 필요 (`golf-realtime` 관할)

## 3. 생성할 스킬 (초안 — 코드베이스 분석 후 통합/분리/추가를 제안하고 내 승인을 받은 뒤 작성)

| 스킬명 | 역할 | kis 대응물 |
|--------|------|-----------|
| `golf-code-change` | 분석 파이프라인(`app_v2.py`) 수정 전 체크리스트. CLAUDE.md의 알고리즘 불변식(argmax 임팩트, 인덱스 매핑, 스무딩 방식, 정규화 vs 원시좌표 구분 등)을 위반하는 수정을 차단 | kis-code-change |
| `golf-analysis-quality` | 영상을 넣고 나온 분석 결과의 품질 판정. 회귀 검증(기존 테스트 영상 3종: 일반.mp4/프로.mp4/프로2.mp4 + reference_db.json)으로 "개선됐다" 성급한 단정 차단 | kis-backtest-result + kis-strategy-review |
| `golf-ui-ux` | Streamlit/웹/앱 UI 작업 규칙. 한글 렌더링 제약, 세션 상태 키 관리, 사용자 동선 원칙 | (신규) |
| `golf-realtime` | 실시간 분석 설계·구현 지침. 온디바이스(브라우저 MediaPipe Tasks JS 등) vs 서버 처리 트레이드오프, 30fps 예산, post-hoc 단계감지 알고리즘의 실시간 전환 제약 | (신규) |
| `golf-platform` | 웹+앱 멀티플랫폼 아키텍처 결정 관제. 단일 파일 구조 탈피·모듈 분리 같은 되돌리기 어려운 결정은 반드시 이 스킬로 판정 후 진행 | kis-go-live |
| `golf-coach-llm` | LLM 코칭 리포트 품질. build_prompt 구조, Explainable(원인→결과) 원칙, 모델별(Gemini/Claude/GPT) 차이 | kis-prompt-engineer 일부 |
| `golf-session-retro` | 세션 종료 시 대화 지식을 CLAUDE.md/스킬/메모리에 영속화 | kis-session-retro |

## 4. 작업 순서

1. `app_v2.py` 전체와 kis-* 스킬 9종을 읽고, 위 초안 대비 **최종 스킬 목록과 각 스킬의 관할 경계**를 표로 제안 → 내 승인 대기
2. 승인 후 `.claude/skills/<스킬명>/SKILL.md`로 생성 (kis 스킬처럼 프로젝트 밖 복사본이 아니라, Claude Code가 자동 인식하는 **프로젝트 `.claude/skills/` 경로**에)
3. 체크리스트 근거는 반드시 실제 코드를 확인해서 `app_v2.py L.줄번호`로 부착 — 추측으로 줄번호를 쓰지 말 것
4. kis 스킬의 항목들이 실제 운용 사고에서 나온 것처럼, 골프 스킬도 처음엔 CLAUDE.md의 확정 사실만 담고, 이후 `golf-session-retro`로 사고 사례를 축적하는 구조로 설계
5. 스킬 생성 완료 후: "코어 보존 + 껍데기 교체" 방향(§2 참조)을 전제로 한 **프로젝트 개선 로드맵**을 제안하고 첫 작업을 무엇으로 할지 물어볼 것. 로드맵의 자연스러운 1단계 후보는 분석 코어 모듈 분리(로직 무변경 + 회귀 검증)다

## 5. 주의

- 스킬 문서는 전부 한국어로 작성
- description은 트리거 문구("스윙 감지가 이상해", "UI 바꿔줘", "실시간으로 하고 싶어" 등 내가 실제로 칠 말)를 포함해야 함
- 각 스킬은 다른 스킬과 겹치는 영역에서 "그건 ○○ 관할"이라고 명시적으로 위임할 것
- 이번 세션에서는 스킬 생성까지만. app_v2.py 코드 수정은 하지 않는다
