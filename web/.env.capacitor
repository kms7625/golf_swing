# Used only for the Capacitor (mobile) build — the Android emulator reaches the
# host machine's loopback via the special alias 10.0.2.2, not localhost.
# For a physical device on the same LAN, replace with the dev machine's LAN IP.
VITE_API_BASE=http://10.0.2.2:8010
